{
  "name": "rootsystems-site",
  "version": "0.1.0",
  "private": true,
  "description": "RootSystems parent brand website — separate from the Offboarding Proof product app.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build:legal": "node scripts/build-legal-data.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "nodemailer": "^9.0.6"
  }
}
